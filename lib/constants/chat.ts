export const CHAT_SEARCH_ERROR_MESSAGE =
  'Beklager, søket feilet. Prøv igjen om litt.';

/** Default locale for POST /chat/turn — matches site language (Norwegian). */
export const CHAT_DEFAULT_LOCALE = 'nb' as const;

/** Locale for landing suggestion chips (homepage + chat landing). */
export const LANDING_SUGGESTIONS_LOCALE = 'nb' as const;
