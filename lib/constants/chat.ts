export const CHAT_SEARCH_ERROR_MESSAGE =
  'Beklager, søket feilet. Prøv igjen om litt.';

/** Default locale for POST /chat/turn — matches site language. */
export const CHAT_DEFAULT_LOCALE = 'en' as const;
