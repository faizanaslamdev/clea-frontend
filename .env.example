# clea-backend (default port 3000). Run Next on 3001: npm run dev
# Local dev: localhost is allowed. Production builds fail without a public, non-loopback URL.
NEXT_PUBLIC_API_URL=http://localhost:3000
